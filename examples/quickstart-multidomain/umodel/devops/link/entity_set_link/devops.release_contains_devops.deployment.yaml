kind: entity_set_link
schema:
  url: "umodel.aliyun.com"
  version: "v0.1.0"
metadata:
  name: "devops.release_contains_devops.deployment"
  domain: devops
  display_name:
    en_us: "Release Contains Deployment"
    zh_cn: "发布包含部署"
  description:
    en_us: "A release groups deployment executions."
    zh_cn: "发布聚合多个部署执行。"
spec:
  src:
    domain: devops
    kind: entity_set
    name: devops.release
  dest:
    domain: devops
    kind: entity_set
    name: devops.deployment
  entity_link_type: contains
