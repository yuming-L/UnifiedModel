kind: entity_set_link
schema:
  url: "umodel.aliyun.com"
  version: "v0.1.0"
metadata:
  name: "service.app_reads_writes_data.store"
  domain: service
  display_name:
    en_us: "Service reads/writes Store"
    zh_cn: "服务读写存储"
  description:
    en_us: "A service reads from or writes to a backing datastore."
    zh_cn: "服务从后端数据存储读取或写入。"
spec:
  src:
    domain: service
    kind: entity_set
    name: service.app
  dest:
    domain: data
    kind: entity_set
    name: data.store
  entity_link_type: reads_writes
