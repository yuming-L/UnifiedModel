# log_set

LogSet is a collection of related log entries that share certain common attributes or characteristics.

**Kind**: `log_set`

> Every element shares the standard envelope `kind` · [metadata](../../shared-types#metadata) · [schema](../../shared-types#schema).

**Inherits**: [telemetry_data](../../shared-types#telemetry_data)

## `spec` fields

_This element has no additional `spec` fields beyond the inherited base._
