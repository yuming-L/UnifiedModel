// Code generated by schema_java_generator_v2.py. DO NOT EDIT.

package com.umodel.schema;

import com.umodel.*;
import com.umodel.shared.*;
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import java.util.HashMap;
import java.util.Map;
import java.util.List;
import java.util.ArrayList;

public class SchemaTest {

    @Test
    public void testAliyunPrometheusV100() {
        AliyunPrometheusV100 obj = new AliyunPrometheusV100();

        assertEquals("aliyun_prometheus", obj.getKind());
        assertTrue(obj instanceof UModelObject);
        assertTrue(obj instanceof UModelCoreObject);
        assertFalse(obj instanceof UModelLinkObject);

        MetadataV1 metadata = new MetadataV1();
        metadata.setName("test_aliyun_prometheus");
        obj.setMetadata(metadata);
        assertEquals(metadata, obj.getMetadata());
        assertEquals("test_aliyun_prometheus", obj.getMetadata().getName());

        SchemaV1 schema = new SchemaV1();
        schema.setVersion("v1.0.0");
        obj.setSchema(schema);
        assertEquals(schema, obj.getSchema());
        assertEquals("v1.0.0", obj.getSchema().getVersion());

        assertNull(obj.validate());
    }

    @Test
    public void testDataLinkV100() {
        DataLinkV100 obj = new DataLinkV100();

        assertEquals("data_link", obj.getKind());
        assertTrue(obj instanceof UModelObject);
        assertTrue(obj instanceof UModelCoreObject);
        assertTrue(obj instanceof UModelLinkObject);

        MetadataV1 metadata = new MetadataV1();
        metadata.setName("test_data_link");
        obj.setMetadata(metadata);
        assertEquals(metadata, obj.getMetadata());
        assertEquals("test_data_link", obj.getMetadata().getName());

        SchemaV1 schema = new SchemaV1();
        schema.setVersion("v1.0.0");
        obj.setSchema(schema);
        assertEquals(schema, obj.getSchema());
        assertEquals("v1.0.0", obj.getSchema().getVersion());

        assertNull(obj.validate());

        assertNull(obj.getSrc());
        assertNull(obj.getDest());
    }

    @Test
    public void testElasticsearchV100() {
        ElasticsearchV100 obj = new ElasticsearchV100();

        assertEquals("elasticsearch", obj.getKind());
        assertTrue(obj instanceof UModelObject);
        assertTrue(obj instanceof UModelCoreObject);
        assertFalse(obj instanceof UModelLinkObject);

        MetadataV1 metadata = new MetadataV1();
        metadata.setName("test_elasticsearch");
        obj.setMetadata(metadata);
        assertEquals(metadata, obj.getMetadata());
        assertEquals("test_elasticsearch", obj.getMetadata().getName());

        SchemaV1 schema = new SchemaV1();
        schema.setVersion("v1.0.0");
        obj.setSchema(schema);
        assertEquals(schema, obj.getSchema());
        assertEquals("v1.0.0", obj.getSchema().getVersion());

        assertNull(obj.validate());
    }

    @Test
    public void testEntitySetV100() {
        EntitySetV100 obj = new EntitySetV100();

        assertEquals("entity_set", obj.getKind());
        assertTrue(obj instanceof UModelObject);
        assertTrue(obj instanceof UModelCoreObject);
        assertFalse(obj instanceof UModelLinkObject);

        MetadataV1 metadata = new MetadataV1();
        metadata.setName("test_entity_set");
        obj.setMetadata(metadata);
        assertEquals(metadata, obj.getMetadata());
        assertEquals("test_entity_set", obj.getMetadata().getName());

        SchemaV1 schema = new SchemaV1();
        schema.setVersion("v1.0.0");
        obj.setSchema(schema);
        assertEquals(schema, obj.getSchema());
        assertEquals("v1.0.0", obj.getSchema().getVersion());

        assertNull(obj.validate());
    }

    @Test
    public void testEntitySetLinkV100() {
        EntitySetLinkV100 obj = new EntitySetLinkV100();

        assertEquals("entity_set_link", obj.getKind());
        assertTrue(obj instanceof UModelObject);
        assertTrue(obj instanceof UModelCoreObject);
        assertTrue(obj instanceof UModelLinkObject);

        MetadataV1 metadata = new MetadataV1();
        metadata.setName("test_entity_set_link");
        obj.setMetadata(metadata);
        assertEquals(metadata, obj.getMetadata());
        assertEquals("test_entity_set_link", obj.getMetadata().getName());

        SchemaV1 schema = new SchemaV1();
        schema.setVersion("v1.0.0");
        obj.setSchema(schema);
        assertEquals(schema, obj.getSchema());
        assertEquals("v1.0.0", obj.getSchema().getVersion());

        assertNull(obj.validate());

        assertNull(obj.getSrc());
        assertNull(obj.getDest());
    }

    @Test
    public void testEntitySourceV100() {
        EntitySourceV100 obj = new EntitySourceV100();

        assertEquals("entity_source", obj.getKind());
        assertTrue(obj instanceof UModelObject);
        assertTrue(obj instanceof UModelCoreObject);
        assertFalse(obj instanceof UModelLinkObject);

        MetadataV1 metadata = new MetadataV1();
        metadata.setName("test_entity_source");
        obj.setMetadata(metadata);
        assertEquals(metadata, obj.getMetadata());
        assertEquals("test_entity_source", obj.getMetadata().getName());

        SchemaV1 schema = new SchemaV1();
        schema.setVersion("v1.0.0");
        obj.setSchema(schema);
        assertEquals(schema, obj.getSchema());
        assertEquals("v1.0.0", obj.getSchema().getVersion());

        assertNull(obj.validate());
    }

    @Test
    public void testEntitySourceLinkV100() {
        EntitySourceLinkV100 obj = new EntitySourceLinkV100();

        assertEquals("entity_source_link", obj.getKind());
        assertTrue(obj instanceof UModelObject);
        assertTrue(obj instanceof UModelCoreObject);
        assertTrue(obj instanceof UModelLinkObject);

        MetadataV1 metadata = new MetadataV1();
        metadata.setName("test_entity_source_link");
        obj.setMetadata(metadata);
        assertEquals(metadata, obj.getMetadata());
        assertEquals("test_entity_source_link", obj.getMetadata().getName());

        SchemaV1 schema = new SchemaV1();
        schema.setVersion("v1.0.0");
        obj.setSchema(schema);
        assertEquals(schema, obj.getSchema());
        assertEquals("v1.0.0", obj.getSchema().getVersion());

        assertNull(obj.validate());

        assertNull(obj.getSrc());
        assertNull(obj.getDest());
    }

    @Test
    public void testEventSetV100() {
        EventSetV100 obj = new EventSetV100();

        assertEquals("event_set", obj.getKind());
        assertTrue(obj instanceof UModelObject);
        assertTrue(obj instanceof UModelCoreObject);
        assertFalse(obj instanceof UModelLinkObject);

        MetadataV1 metadata = new MetadataV1();
        metadata.setName("test_event_set");
        obj.setMetadata(metadata);
        assertEquals(metadata, obj.getMetadata());
        assertEquals("test_event_set", obj.getMetadata().getName());

        SchemaV1 schema = new SchemaV1();
        schema.setVersion("v1.0.0");
        obj.setSchema(schema);
        assertEquals(schema, obj.getSchema());
        assertEquals("v1.0.0", obj.getSchema().getVersion());

        assertNull(obj.validate());
    }

    @Test
    public void testExplorerV100() {
        ExplorerV100 obj = new ExplorerV100();

        assertEquals("explorer", obj.getKind());
        assertTrue(obj instanceof UModelObject);
        assertTrue(obj instanceof UModelCoreObject);
        assertFalse(obj instanceof UModelLinkObject);

        MetadataV1 metadata = new MetadataV1();
        metadata.setName("test_explorer");
        obj.setMetadata(metadata);
        assertEquals(metadata, obj.getMetadata());
        assertEquals("test_explorer", obj.getMetadata().getName());

        SchemaV1 schema = new SchemaV1();
        schema.setVersion("v1.0.0");
        obj.setSchema(schema);
        assertEquals(schema, obj.getSchema());
        assertEquals("v1.0.0", obj.getSchema().getVersion());

        assertNull(obj.validate());
    }

    @Test
    public void testExplorerLinkV100() {
        ExplorerLinkV100 obj = new ExplorerLinkV100();

        assertEquals("explorer_link", obj.getKind());
        assertTrue(obj instanceof UModelObject);
        assertTrue(obj instanceof UModelCoreObject);
        assertTrue(obj instanceof UModelLinkObject);

        MetadataV1 metadata = new MetadataV1();
        metadata.setName("test_explorer_link");
        obj.setMetadata(metadata);
        assertEquals(metadata, obj.getMetadata());
        assertEquals("test_explorer_link", obj.getMetadata().getName());

        SchemaV1 schema = new SchemaV1();
        schema.setVersion("v1.0.0");
        obj.setSchema(schema);
        assertEquals(schema, obj.getSchema());
        assertEquals("v1.0.0", obj.getSchema().getVersion());

        assertNull(obj.validate());

        assertNull(obj.getSrc());
        assertNull(obj.getDest());
    }

    @Test
    public void testExternalStorageV100() {
        ExternalStorageV100 obj = new ExternalStorageV100();

        assertEquals("external_storage", obj.getKind());
        assertTrue(obj instanceof UModelObject);
        assertTrue(obj instanceof UModelCoreObject);
        assertFalse(obj instanceof UModelLinkObject);

        MetadataV1 metadata = new MetadataV1();
        metadata.setName("test_external_storage");
        obj.setMetadata(metadata);
        assertEquals(metadata, obj.getMetadata());
        assertEquals("test_external_storage", obj.getMetadata().getName());

        SchemaV1 schema = new SchemaV1();
        schema.setVersion("v1.0.0");
        obj.setSchema(schema);
        assertEquals(schema, obj.getSchema());
        assertEquals("v1.0.0", obj.getSchema().getVersion());

        assertNull(obj.validate());
    }

    @Test
    public void testLogSetV100() {
        LogSetV100 obj = new LogSetV100();

        assertEquals("log_set", obj.getKind());
        assertTrue(obj instanceof UModelObject);
        assertTrue(obj instanceof UModelCoreObject);
        assertFalse(obj instanceof UModelLinkObject);

        MetadataV1 metadata = new MetadataV1();
        metadata.setName("test_log_set");
        obj.setMetadata(metadata);
        assertEquals(metadata, obj.getMetadata());
        assertEquals("test_log_set", obj.getMetadata().getName());

        SchemaV1 schema = new SchemaV1();
        schema.setVersion("v1.0.0");
        obj.setSchema(schema);
        assertEquals(schema, obj.getSchema());
        assertEquals("v1.0.0", obj.getSchema().getVersion());

        assertNull(obj.validate());
    }

    @Test
    public void testMetricSetV100() {
        MetricSetV100 obj = new MetricSetV100();

        assertEquals("metric_set", obj.getKind());
        assertTrue(obj instanceof UModelObject);
        assertTrue(obj instanceof UModelCoreObject);
        assertFalse(obj instanceof UModelLinkObject);

        MetadataV1 metadata = new MetadataV1();
        metadata.setName("test_metric_set");
        obj.setMetadata(metadata);
        assertEquals(metadata, obj.getMetadata());
        assertEquals("test_metric_set", obj.getMetadata().getName());

        SchemaV1 schema = new SchemaV1();
        schema.setVersion("v1.0.0");
        obj.setSchema(schema);
        assertEquals(schema, obj.getSchema());
        assertEquals("v1.0.0", obj.getSchema().getVersion());

        assertNull(obj.validate());
    }

    @Test
    public void testMysqlV100() {
        MysqlV100 obj = new MysqlV100();

        assertEquals("mysql", obj.getKind());
        assertTrue(obj instanceof UModelObject);
        assertTrue(obj instanceof UModelCoreObject);
        assertFalse(obj instanceof UModelLinkObject);

        MetadataV1 metadata = new MetadataV1();
        metadata.setName("test_mysql");
        obj.setMetadata(metadata);
        assertEquals(metadata, obj.getMetadata());
        assertEquals("test_mysql", obj.getMetadata().getName());

        SchemaV1 schema = new SchemaV1();
        schema.setVersion("v1.0.0");
        obj.setSchema(schema);
        assertEquals(schema, obj.getSchema());
        assertEquals("v1.0.0", obj.getSchema().getVersion());

        assertNull(obj.validate());
    }

    @Test
    public void testProfileSetV100() {
        ProfileSetV100 obj = new ProfileSetV100();

        assertEquals("profile_set", obj.getKind());
        assertTrue(obj instanceof UModelObject);
        assertTrue(obj instanceof UModelCoreObject);
        assertFalse(obj instanceof UModelLinkObject);

        MetadataV1 metadata = new MetadataV1();
        metadata.setName("test_profile_set");
        obj.setMetadata(metadata);
        assertEquals(metadata, obj.getMetadata());
        assertEquals("test_profile_set", obj.getMetadata().getName());

        SchemaV1 schema = new SchemaV1();
        schema.setVersion("v1.0.0");
        obj.setSchema(schema);
        assertEquals(schema, obj.getSchema());
        assertEquals("v1.0.0", obj.getSchema().getVersion());

        assertNull(obj.validate());
    }

    @Test
    public void testPrometheusV100() {
        PrometheusV100 obj = new PrometheusV100();

        assertEquals("prometheus", obj.getKind());
        assertTrue(obj instanceof UModelObject);
        assertTrue(obj instanceof UModelCoreObject);
        assertFalse(obj instanceof UModelLinkObject);

        MetadataV1 metadata = new MetadataV1();
        metadata.setName("test_prometheus");
        obj.setMetadata(metadata);
        assertEquals(metadata, obj.getMetadata());
        assertEquals("test_prometheus", obj.getMetadata().getName());

        SchemaV1 schema = new SchemaV1();
        schema.setVersion("v1.0.0");
        obj.setSchema(schema);
        assertEquals(schema, obj.getSchema());
        assertEquals("v1.0.0", obj.getSchema().getVersion());

        assertNull(obj.validate());
    }

    @Test
    public void testRunbookLinkV100() {
        RunbookLinkV100 obj = new RunbookLinkV100();

        assertEquals("runbook_link", obj.getKind());
        assertTrue(obj instanceof UModelObject);
        assertTrue(obj instanceof UModelCoreObject);
        assertTrue(obj instanceof UModelLinkObject);

        MetadataV1 metadata = new MetadataV1();
        metadata.setName("test_runbook_link");
        obj.setMetadata(metadata);
        assertEquals(metadata, obj.getMetadata());
        assertEquals("test_runbook_link", obj.getMetadata().getName());

        SchemaV1 schema = new SchemaV1();
        schema.setVersion("v1.0.0");
        obj.setSchema(schema);
        assertEquals(schema, obj.getSchema());
        assertEquals("v1.0.0", obj.getSchema().getVersion());

        assertNull(obj.validate());

        assertNull(obj.getSrc());
        assertNull(obj.getDest());
    }

    @Test
    public void testRunbookSetV100() {
        RunbookSetV100 obj = new RunbookSetV100();

        assertEquals("runbook_set", obj.getKind());
        assertTrue(obj instanceof UModelObject);
        assertTrue(obj instanceof UModelCoreObject);
        assertFalse(obj instanceof UModelLinkObject);

        MetadataV1 metadata = new MetadataV1();
        metadata.setName("test_runbook_set");
        obj.setMetadata(metadata);
        assertEquals(metadata, obj.getMetadata());
        assertEquals("test_runbook_set", obj.getMetadata().getName());

        SchemaV1 schema = new SchemaV1();
        schema.setVersion("v1.0.0");
        obj.setSchema(schema);
        assertEquals(schema, obj.getSchema());
        assertEquals("v1.0.0", obj.getSchema().getVersion());

        assertNull(obj.validate());
    }

    @Test
    public void testSlsEntitystoreV100() {
        SlsEntitystoreV100 obj = new SlsEntitystoreV100();

        assertEquals("sls_entitystore", obj.getKind());
        assertTrue(obj instanceof UModelObject);
        assertTrue(obj instanceof UModelCoreObject);
        assertFalse(obj instanceof UModelLinkObject);

        MetadataV1 metadata = new MetadataV1();
        metadata.setName("test_sls_entitystore");
        obj.setMetadata(metadata);
        assertEquals(metadata, obj.getMetadata());
        assertEquals("test_sls_entitystore", obj.getMetadata().getName());

        SchemaV1 schema = new SchemaV1();
        schema.setVersion("v1.0.0");
        obj.setSchema(schema);
        assertEquals(schema, obj.getSchema());
        assertEquals("v1.0.0", obj.getSchema().getVersion());

        assertNull(obj.validate());
    }

    @Test
    public void testSlsLogstoreV100() {
        SlsLogstoreV100 obj = new SlsLogstoreV100();

        assertEquals("sls_logstore", obj.getKind());
        assertTrue(obj instanceof UModelObject);
        assertTrue(obj instanceof UModelCoreObject);
        assertFalse(obj instanceof UModelLinkObject);

        MetadataV1 metadata = new MetadataV1();
        metadata.setName("test_sls_logstore");
        obj.setMetadata(metadata);
        assertEquals(metadata, obj.getMetadata());
        assertEquals("test_sls_logstore", obj.getMetadata().getName());

        SchemaV1 schema = new SchemaV1();
        schema.setVersion("v1.0.0");
        obj.setSchema(schema);
        assertEquals(schema, obj.getSchema());
        assertEquals("v1.0.0", obj.getSchema().getVersion());

        assertNull(obj.validate());
    }

    @Test
    public void testSlsMetricstoreV100() {
        SlsMetricstoreV100 obj = new SlsMetricstoreV100();

        assertEquals("sls_metricstore", obj.getKind());
        assertTrue(obj instanceof UModelObject);
        assertTrue(obj instanceof UModelCoreObject);
        assertFalse(obj instanceof UModelLinkObject);

        MetadataV1 metadata = new MetadataV1();
        metadata.setName("test_sls_metricstore");
        obj.setMetadata(metadata);
        assertEquals(metadata, obj.getMetadata());
        assertEquals("test_sls_metricstore", obj.getMetadata().getName());

        SchemaV1 schema = new SchemaV1();
        schema.setVersion("v1.0.0");
        obj.setSchema(schema);
        assertEquals(schema, obj.getSchema());
        assertEquals("v1.0.0", obj.getSchema().getVersion());

        assertNull(obj.validate());
    }

    @Test
    public void testStorageLinkV100() {
        StorageLinkV100 obj = new StorageLinkV100();

        assertEquals("storage_link", obj.getKind());
        assertTrue(obj instanceof UModelObject);
        assertTrue(obj instanceof UModelCoreObject);
        assertTrue(obj instanceof UModelLinkObject);

        MetadataV1 metadata = new MetadataV1();
        metadata.setName("test_storage_link");
        obj.setMetadata(metadata);
        assertEquals(metadata, obj.getMetadata());
        assertEquals("test_storage_link", obj.getMetadata().getName());

        SchemaV1 schema = new SchemaV1();
        schema.setVersion("v1.0.0");
        obj.setSchema(schema);
        assertEquals(schema, obj.getSchema());
        assertEquals("v1.0.0", obj.getSchema().getVersion());

        assertNull(obj.validate());

        assertNull(obj.getSrc());
        assertNull(obj.getDest());
    }

    @Test
    public void testTraceSetV100() {
        TraceSetV100 obj = new TraceSetV100();

        assertEquals("trace_set", obj.getKind());
        assertTrue(obj instanceof UModelObject);
        assertTrue(obj instanceof UModelCoreObject);
        assertFalse(obj instanceof UModelLinkObject);

        MetadataV1 metadata = new MetadataV1();
        metadata.setName("test_trace_set");
        obj.setMetadata(metadata);
        assertEquals(metadata, obj.getMetadata());
        assertEquals("test_trace_set", obj.getMetadata().getName());

        SchemaV1 schema = new SchemaV1();
        schema.setVersion("v1.0.0");
        obj.setSchema(schema);
        assertEquals(schema, obj.getSchema());
        assertEquals("v1.0.0", obj.getSchema().getVersion());

        assertNull(obj.validate());
    }
}