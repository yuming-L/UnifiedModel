// Code generated by schema_java_generator_v2.py. DO NOT EDIT.

package com.umodel.integration;

import com.umodel.*;
import com.umodel.schema.*;
import com.umodel.shared.*;
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

import java.util.Map;
import java.util.List;

public class TestParsingIntegrationTest {

    @Test
    public void testAliyunPrometheusJsonParsing() {
        String json = "{\"kind\":\"aliyun_prometheus\",\"schema\":{\"url\":\"umodel.aliyun.com\",\"version\":\"v1.0.0\"},\"metadata\":{\"name\":\"test_aliyun_prometheus\",\"display_name\":{\"zh_cn\":\"测试aliyun_prometheus\",\"en_us\":\"Test aliyun_prometheus\"},\"domain\":\"test.domain\",\"launch_stage\":\"ga\",\"tags\":{\"category\":\"test\"}},\"spec\":{\"_data\":\"placeholder\"}}";

        UModelCoreObject obj = UModel.parseUModelJson(json);
        assertNotNull(obj);
        assertTrue(obj instanceof AliyunPrometheusV100);
        assertEquals("aliyun_prometheus", obj.getKind());

        AliyunPrometheusV100 typed = (AliyunPrometheusV100) obj;
        SchemaV1 schema = typed.getSchema();
        assertNotNull(schema);
        assertEquals("umodel.aliyun.com", schema.getUrl());
        assertEquals("v1.0.0", schema.getVersion());

        MetadataV1 metadata = typed.getMetadata();
        assertNotNull(metadata);
        assertEquals("test_aliyun_prometheus", metadata.getName());
        assertEquals("test.domain", metadata.getDomain());
        assertEquals("ga", metadata.getLaunchStage());

        SemanticStringV1 displayName = metadata.getDisplayName();
        assertNotNull(displayName);
        assertEquals("Test aliyun_prometheus", displayName.getEnUs());

        Map<String, String> tags = metadata.getTags();
        assertNotNull(tags);
        assertEquals("test", tags.get("category"));
    }

    @Test
    public void testDataLinkJsonParsing() {
        String json = "{\"kind\":\"data_link\",\"schema\":{\"url\":\"umodel.aliyun.com\",\"version\":\"v1.0.0\"},\"metadata\":{\"name\":\"test_data_link\",\"display_name\":{\"zh_cn\":\"测试data_link\",\"en_us\":\"Test data_link\"},\"domain\":\"test.domain\",\"launch_stage\":\"ga\",\"tags\":{\"category\":\"test\"}},\"spec\":{\"_data\":\"placeholder\"}}";

        UModelCoreObject obj = UModel.parseUModelJson(json);
        assertNotNull(obj);
        assertTrue(obj instanceof DataLinkV100);
        assertEquals("data_link", obj.getKind());

        DataLinkV100 typed = (DataLinkV100) obj;
        SchemaV1 schema = typed.getSchema();
        assertNotNull(schema);
        assertEquals("umodel.aliyun.com", schema.getUrl());
        assertEquals("v1.0.0", schema.getVersion());

        MetadataV1 metadata = typed.getMetadata();
        assertNotNull(metadata);
        assertEquals("test_data_link", metadata.getName());
        assertEquals("test.domain", metadata.getDomain());
        assertEquals("ga", metadata.getLaunchStage());

        SemanticStringV1 displayName = metadata.getDisplayName();
        assertNotNull(displayName);
        assertEquals("Test data_link", displayName.getEnUs());

        Map<String, String> tags = metadata.getTags();
        assertNotNull(tags);
        assertEquals("test", tags.get("category"));
    }

    @Test
    public void testElasticsearchJsonParsing() {
        String json = "{\"kind\":\"elasticsearch\",\"schema\":{\"url\":\"umodel.aliyun.com\",\"version\":\"v1.0.0\"},\"metadata\":{\"name\":\"test_elasticsearch\",\"display_name\":{\"zh_cn\":\"测试elasticsearch\",\"en_us\":\"Test elasticsearch\"},\"domain\":\"test.domain\",\"launch_stage\":\"ga\",\"tags\":{\"category\":\"test\"}},\"spec\":{\"_data\":\"placeholder\"}}";

        UModelCoreObject obj = UModel.parseUModelJson(json);
        assertNotNull(obj);
        assertTrue(obj instanceof ElasticsearchV100);
        assertEquals("elasticsearch", obj.getKind());

        ElasticsearchV100 typed = (ElasticsearchV100) obj;
        SchemaV1 schema = typed.getSchema();
        assertNotNull(schema);
        assertEquals("umodel.aliyun.com", schema.getUrl());
        assertEquals("v1.0.0", schema.getVersion());

        MetadataV1 metadata = typed.getMetadata();
        assertNotNull(metadata);
        assertEquals("test_elasticsearch", metadata.getName());
        assertEquals("test.domain", metadata.getDomain());
        assertEquals("ga", metadata.getLaunchStage());

        SemanticStringV1 displayName = metadata.getDisplayName();
        assertNotNull(displayName);
        assertEquals("Test elasticsearch", displayName.getEnUs());

        Map<String, String> tags = metadata.getTags();
        assertNotNull(tags);
        assertEquals("test", tags.get("category"));
    }

    @Test
    public void testEntitySetJsonParsing() {
        String json = "{\"kind\":\"entity_set\",\"schema\":{\"url\":\"umodel.aliyun.com\",\"version\":\"v1.0.0\"},\"metadata\":{\"name\":\"test_entity_set\",\"display_name\":{\"zh_cn\":\"测试entity_set\",\"en_us\":\"Test entity_set\"},\"domain\":\"test.domain\",\"launch_stage\":\"ga\",\"tags\":{\"category\":\"test\"}},\"spec\":{\"_data\":\"placeholder\"}}";

        UModelCoreObject obj = UModel.parseUModelJson(json);
        assertNotNull(obj);
        assertTrue(obj instanceof EntitySetV100);
        assertEquals("entity_set", obj.getKind());

        EntitySetV100 typed = (EntitySetV100) obj;
        SchemaV1 schema = typed.getSchema();
        assertNotNull(schema);
        assertEquals("umodel.aliyun.com", schema.getUrl());
        assertEquals("v1.0.0", schema.getVersion());

        MetadataV1 metadata = typed.getMetadata();
        assertNotNull(metadata);
        assertEquals("test_entity_set", metadata.getName());
        assertEquals("test.domain", metadata.getDomain());
        assertEquals("ga", metadata.getLaunchStage());

        SemanticStringV1 displayName = metadata.getDisplayName();
        assertNotNull(displayName);
        assertEquals("Test entity_set", displayName.getEnUs());

        Map<String, String> tags = metadata.getTags();
        assertNotNull(tags);
        assertEquals("test", tags.get("category"));
    }

    @Test
    public void testEntitySetLinkJsonParsing() {
        String json = "{\"kind\":\"entity_set_link\",\"schema\":{\"url\":\"umodel.aliyun.com\",\"version\":\"v1.0.0\"},\"metadata\":{\"name\":\"test_entity_set_link\",\"display_name\":{\"zh_cn\":\"测试entity_set_link\",\"en_us\":\"Test entity_set_link\"},\"domain\":\"test.domain\",\"launch_stage\":\"ga\",\"tags\":{\"category\":\"test\"}},\"spec\":{\"_data\":\"placeholder\"}}";

        UModelCoreObject obj = UModel.parseUModelJson(json);
        assertNotNull(obj);
        assertTrue(obj instanceof EntitySetLinkV100);
        assertEquals("entity_set_link", obj.getKind());

        EntitySetLinkV100 typed = (EntitySetLinkV100) obj;
        SchemaV1 schema = typed.getSchema();
        assertNotNull(schema);
        assertEquals("umodel.aliyun.com", schema.getUrl());
        assertEquals("v1.0.0", schema.getVersion());

        MetadataV1 metadata = typed.getMetadata();
        assertNotNull(metadata);
        assertEquals("test_entity_set_link", metadata.getName());
        assertEquals("test.domain", metadata.getDomain());
        assertEquals("ga", metadata.getLaunchStage());

        SemanticStringV1 displayName = metadata.getDisplayName();
        assertNotNull(displayName);
        assertEquals("Test entity_set_link", displayName.getEnUs());

        Map<String, String> tags = metadata.getTags();
        assertNotNull(tags);
        assertEquals("test", tags.get("category"));
    }

    @Test
    public void testEntitySourceJsonParsing() {
        String json = "{\"kind\":\"entity_source\",\"schema\":{\"url\":\"umodel.aliyun.com\",\"version\":\"v1.0.0\"},\"metadata\":{\"name\":\"test_entity_source\",\"display_name\":{\"zh_cn\":\"测试entity_source\",\"en_us\":\"Test entity_source\"},\"domain\":\"test.domain\",\"launch_stage\":\"ga\",\"tags\":{\"category\":\"test\"}},\"spec\":{\"_data\":\"placeholder\"}}";

        UModelCoreObject obj = UModel.parseUModelJson(json);
        assertNotNull(obj);
        assertTrue(obj instanceof EntitySourceV100);
        assertEquals("entity_source", obj.getKind());

        EntitySourceV100 typed = (EntitySourceV100) obj;
        SchemaV1 schema = typed.getSchema();
        assertNotNull(schema);
        assertEquals("umodel.aliyun.com", schema.getUrl());
        assertEquals("v1.0.0", schema.getVersion());

        MetadataV1 metadata = typed.getMetadata();
        assertNotNull(metadata);
        assertEquals("test_entity_source", metadata.getName());
        assertEquals("test.domain", metadata.getDomain());
        assertEquals("ga", metadata.getLaunchStage());

        SemanticStringV1 displayName = metadata.getDisplayName();
        assertNotNull(displayName);
        assertEquals("Test entity_source", displayName.getEnUs());

        Map<String, String> tags = metadata.getTags();
        assertNotNull(tags);
        assertEquals("test", tags.get("category"));
    }

    @Test
    public void testEntitySourceLinkJsonParsing() {
        String json = "{\"kind\":\"entity_source_link\",\"schema\":{\"url\":\"umodel.aliyun.com\",\"version\":\"v1.0.0\"},\"metadata\":{\"name\":\"test_entity_source_link\",\"display_name\":{\"zh_cn\":\"测试entity_source_link\",\"en_us\":\"Test entity_source_link\"},\"domain\":\"test.domain\",\"launch_stage\":\"ga\",\"tags\":{\"category\":\"test\"}},\"spec\":{\"_data\":\"placeholder\"}}";

        UModelCoreObject obj = UModel.parseUModelJson(json);
        assertNotNull(obj);
        assertTrue(obj instanceof EntitySourceLinkV100);
        assertEquals("entity_source_link", obj.getKind());

        EntitySourceLinkV100 typed = (EntitySourceLinkV100) obj;
        SchemaV1 schema = typed.getSchema();
        assertNotNull(schema);
        assertEquals("umodel.aliyun.com", schema.getUrl());
        assertEquals("v1.0.0", schema.getVersion());

        MetadataV1 metadata = typed.getMetadata();
        assertNotNull(metadata);
        assertEquals("test_entity_source_link", metadata.getName());
        assertEquals("test.domain", metadata.getDomain());
        assertEquals("ga", metadata.getLaunchStage());

        SemanticStringV1 displayName = metadata.getDisplayName();
        assertNotNull(displayName);
        assertEquals("Test entity_source_link", displayName.getEnUs());

        Map<String, String> tags = metadata.getTags();
        assertNotNull(tags);
        assertEquals("test", tags.get("category"));
    }

    @Test
    public void testEventSetJsonParsing() {
        String json = "{\"kind\":\"event_set\",\"schema\":{\"url\":\"umodel.aliyun.com\",\"version\":\"v1.0.0\"},\"metadata\":{\"name\":\"test_event_set\",\"display_name\":{\"zh_cn\":\"测试event_set\",\"en_us\":\"Test event_set\"},\"domain\":\"test.domain\",\"launch_stage\":\"ga\",\"tags\":{\"category\":\"test\"}},\"spec\":{\"_data\":\"placeholder\"}}";

        UModelCoreObject obj = UModel.parseUModelJson(json);
        assertNotNull(obj);
        assertTrue(obj instanceof EventSetV100);
        assertEquals("event_set", obj.getKind());

        EventSetV100 typed = (EventSetV100) obj;
        SchemaV1 schema = typed.getSchema();
        assertNotNull(schema);
        assertEquals("umodel.aliyun.com", schema.getUrl());
        assertEquals("v1.0.0", schema.getVersion());

        MetadataV1 metadata = typed.getMetadata();
        assertNotNull(metadata);
        assertEquals("test_event_set", metadata.getName());
        assertEquals("test.domain", metadata.getDomain());
        assertEquals("ga", metadata.getLaunchStage());

        SemanticStringV1 displayName = metadata.getDisplayName();
        assertNotNull(displayName);
        assertEquals("Test event_set", displayName.getEnUs());

        Map<String, String> tags = metadata.getTags();
        assertNotNull(tags);
        assertEquals("test", tags.get("category"));
    }

    @Test
    public void testExplorerJsonParsing() {
        String json = "{\"kind\":\"explorer\",\"schema\":{\"url\":\"umodel.aliyun.com\",\"version\":\"v1.0.0\"},\"metadata\":{\"name\":\"test_explorer\",\"display_name\":{\"zh_cn\":\"测试explorer\",\"en_us\":\"Test explorer\"},\"domain\":\"test.domain\",\"launch_stage\":\"ga\",\"tags\":{\"category\":\"test\"}},\"spec\":{\"_data\":\"placeholder\"}}";

        UModelCoreObject obj = UModel.parseUModelJson(json);
        assertNotNull(obj);
        assertTrue(obj instanceof ExplorerV100);
        assertEquals("explorer", obj.getKind());

        ExplorerV100 typed = (ExplorerV100) obj;
        SchemaV1 schema = typed.getSchema();
        assertNotNull(schema);
        assertEquals("umodel.aliyun.com", schema.getUrl());
        assertEquals("v1.0.0", schema.getVersion());

        MetadataV1 metadata = typed.getMetadata();
        assertNotNull(metadata);
        assertEquals("test_explorer", metadata.getName());
        assertEquals("test.domain", metadata.getDomain());
        assertEquals("ga", metadata.getLaunchStage());

        SemanticStringV1 displayName = metadata.getDisplayName();
        assertNotNull(displayName);
        assertEquals("Test explorer", displayName.getEnUs());

        Map<String, String> tags = metadata.getTags();
        assertNotNull(tags);
        assertEquals("test", tags.get("category"));
    }

    @Test
    public void testExplorerLinkJsonParsing() {
        String json = "{\"kind\":\"explorer_link\",\"schema\":{\"url\":\"umodel.aliyun.com\",\"version\":\"v1.0.0\"},\"metadata\":{\"name\":\"test_explorer_link\",\"display_name\":{\"zh_cn\":\"测试explorer_link\",\"en_us\":\"Test explorer_link\"},\"domain\":\"test.domain\",\"launch_stage\":\"ga\",\"tags\":{\"category\":\"test\"}},\"spec\":{\"_data\":\"placeholder\"}}";

        UModelCoreObject obj = UModel.parseUModelJson(json);
        assertNotNull(obj);
        assertTrue(obj instanceof ExplorerLinkV100);
        assertEquals("explorer_link", obj.getKind());

        ExplorerLinkV100 typed = (ExplorerLinkV100) obj;
        SchemaV1 schema = typed.getSchema();
        assertNotNull(schema);
        assertEquals("umodel.aliyun.com", schema.getUrl());
        assertEquals("v1.0.0", schema.getVersion());

        MetadataV1 metadata = typed.getMetadata();
        assertNotNull(metadata);
        assertEquals("test_explorer_link", metadata.getName());
        assertEquals("test.domain", metadata.getDomain());
        assertEquals("ga", metadata.getLaunchStage());

        SemanticStringV1 displayName = metadata.getDisplayName();
        assertNotNull(displayName);
        assertEquals("Test explorer_link", displayName.getEnUs());

        Map<String, String> tags = metadata.getTags();
        assertNotNull(tags);
        assertEquals("test", tags.get("category"));
    }

    @Test
    public void testExternalStorageJsonParsing() {
        String json = "{\"kind\":\"external_storage\",\"schema\":{\"url\":\"umodel.aliyun.com\",\"version\":\"v1.0.0\"},\"metadata\":{\"name\":\"test_external_storage\",\"display_name\":{\"zh_cn\":\"测试external_storage\",\"en_us\":\"Test external_storage\"},\"domain\":\"test.domain\",\"launch_stage\":\"ga\",\"tags\":{\"category\":\"test\"}},\"spec\":{\"_data\":\"placeholder\"}}";

        UModelCoreObject obj = UModel.parseUModelJson(json);
        assertNotNull(obj);
        assertTrue(obj instanceof ExternalStorageV100);
        assertEquals("external_storage", obj.getKind());

        ExternalStorageV100 typed = (ExternalStorageV100) obj;
        SchemaV1 schema = typed.getSchema();
        assertNotNull(schema);
        assertEquals("umodel.aliyun.com", schema.getUrl());
        assertEquals("v1.0.0", schema.getVersion());

        MetadataV1 metadata = typed.getMetadata();
        assertNotNull(metadata);
        assertEquals("test_external_storage", metadata.getName());
        assertEquals("test.domain", metadata.getDomain());
        assertEquals("ga", metadata.getLaunchStage());

        SemanticStringV1 displayName = metadata.getDisplayName();
        assertNotNull(displayName);
        assertEquals("Test external_storage", displayName.getEnUs());

        Map<String, String> tags = metadata.getTags();
        assertNotNull(tags);
        assertEquals("test", tags.get("category"));
    }

    @Test
    public void testLogSetJsonParsing() {
        String json = "{\"kind\":\"log_set\",\"schema\":{\"url\":\"umodel.aliyun.com\",\"version\":\"v1.0.0\"},\"metadata\":{\"name\":\"test_log_set\",\"display_name\":{\"zh_cn\":\"测试log_set\",\"en_us\":\"Test log_set\"},\"domain\":\"test.domain\",\"launch_stage\":\"ga\",\"tags\":{\"category\":\"test\"}},\"spec\":{\"_data\":\"placeholder\"}}";

        UModelCoreObject obj = UModel.parseUModelJson(json);
        assertNotNull(obj);
        assertTrue(obj instanceof LogSetV100);
        assertEquals("log_set", obj.getKind());

        LogSetV100 typed = (LogSetV100) obj;
        SchemaV1 schema = typed.getSchema();
        assertNotNull(schema);
        assertEquals("umodel.aliyun.com", schema.getUrl());
        assertEquals("v1.0.0", schema.getVersion());

        MetadataV1 metadata = typed.getMetadata();
        assertNotNull(metadata);
        assertEquals("test_log_set", metadata.getName());
        assertEquals("test.domain", metadata.getDomain());
        assertEquals("ga", metadata.getLaunchStage());

        SemanticStringV1 displayName = metadata.getDisplayName();
        assertNotNull(displayName);
        assertEquals("Test log_set", displayName.getEnUs());

        Map<String, String> tags = metadata.getTags();
        assertNotNull(tags);
        assertEquals("test", tags.get("category"));
    }

    @Test
    public void testMetricSetJsonParsing() {
        String json = "{\"kind\":\"metric_set\",\"schema\":{\"url\":\"umodel.aliyun.com\",\"version\":\"v1.0.0\"},\"metadata\":{\"name\":\"test_metric_set\",\"display_name\":{\"zh_cn\":\"测试metric_set\",\"en_us\":\"Test metric_set\"},\"domain\":\"test.domain\",\"launch_stage\":\"ga\",\"tags\":{\"category\":\"test\"}},\"spec\":{\"_data\":\"placeholder\"}}";

        UModelCoreObject obj = UModel.parseUModelJson(json);
        assertNotNull(obj);
        assertTrue(obj instanceof MetricSetV100);
        assertEquals("metric_set", obj.getKind());

        MetricSetV100 typed = (MetricSetV100) obj;
        SchemaV1 schema = typed.getSchema();
        assertNotNull(schema);
        assertEquals("umodel.aliyun.com", schema.getUrl());
        assertEquals("v1.0.0", schema.getVersion());

        MetadataV1 metadata = typed.getMetadata();
        assertNotNull(metadata);
        assertEquals("test_metric_set", metadata.getName());
        assertEquals("test.domain", metadata.getDomain());
        assertEquals("ga", metadata.getLaunchStage());

        SemanticStringV1 displayName = metadata.getDisplayName();
        assertNotNull(displayName);
        assertEquals("Test metric_set", displayName.getEnUs());

        Map<String, String> tags = metadata.getTags();
        assertNotNull(tags);
        assertEquals("test", tags.get("category"));
    }

    @Test
    public void testMysqlJsonParsing() {
        String json = "{\"kind\":\"mysql\",\"schema\":{\"url\":\"umodel.aliyun.com\",\"version\":\"v1.0.0\"},\"metadata\":{\"name\":\"test_mysql\",\"display_name\":{\"zh_cn\":\"测试mysql\",\"en_us\":\"Test mysql\"},\"domain\":\"test.domain\",\"launch_stage\":\"ga\",\"tags\":{\"category\":\"test\"}},\"spec\":{\"_data\":\"placeholder\"}}";

        UModelCoreObject obj = UModel.parseUModelJson(json);
        assertNotNull(obj);
        assertTrue(obj instanceof MysqlV100);
        assertEquals("mysql", obj.getKind());

        MysqlV100 typed = (MysqlV100) obj;
        SchemaV1 schema = typed.getSchema();
        assertNotNull(schema);
        assertEquals("umodel.aliyun.com", schema.getUrl());
        assertEquals("v1.0.0", schema.getVersion());

        MetadataV1 metadata = typed.getMetadata();
        assertNotNull(metadata);
        assertEquals("test_mysql", metadata.getName());
        assertEquals("test.domain", metadata.getDomain());
        assertEquals("ga", metadata.getLaunchStage());

        SemanticStringV1 displayName = metadata.getDisplayName();
        assertNotNull(displayName);
        assertEquals("Test mysql", displayName.getEnUs());

        Map<String, String> tags = metadata.getTags();
        assertNotNull(tags);
        assertEquals("test", tags.get("category"));
    }

    @Test
    public void testProfileSetJsonParsing() {
        String json = "{\"kind\":\"profile_set\",\"schema\":{\"url\":\"umodel.aliyun.com\",\"version\":\"v1.0.0\"},\"metadata\":{\"name\":\"test_profile_set\",\"display_name\":{\"zh_cn\":\"测试profile_set\",\"en_us\":\"Test profile_set\"},\"domain\":\"test.domain\",\"launch_stage\":\"ga\",\"tags\":{\"category\":\"test\"}},\"spec\":{\"_data\":\"placeholder\"}}";

        UModelCoreObject obj = UModel.parseUModelJson(json);
        assertNotNull(obj);
        assertTrue(obj instanceof ProfileSetV100);
        assertEquals("profile_set", obj.getKind());

        ProfileSetV100 typed = (ProfileSetV100) obj;
        SchemaV1 schema = typed.getSchema();
        assertNotNull(schema);
        assertEquals("umodel.aliyun.com", schema.getUrl());
        assertEquals("v1.0.0", schema.getVersion());

        MetadataV1 metadata = typed.getMetadata();
        assertNotNull(metadata);
        assertEquals("test_profile_set", metadata.getName());
        assertEquals("test.domain", metadata.getDomain());
        assertEquals("ga", metadata.getLaunchStage());

        SemanticStringV1 displayName = metadata.getDisplayName();
        assertNotNull(displayName);
        assertEquals("Test profile_set", displayName.getEnUs());

        Map<String, String> tags = metadata.getTags();
        assertNotNull(tags);
        assertEquals("test", tags.get("category"));
    }

    @Test
    public void testPrometheusJsonParsing() {
        String json = "{\"kind\":\"prometheus\",\"schema\":{\"url\":\"umodel.aliyun.com\",\"version\":\"v1.0.0\"},\"metadata\":{\"name\":\"test_prometheus\",\"display_name\":{\"zh_cn\":\"测试prometheus\",\"en_us\":\"Test prometheus\"},\"domain\":\"test.domain\",\"launch_stage\":\"ga\",\"tags\":{\"category\":\"test\"}},\"spec\":{\"_data\":\"placeholder\"}}";

        UModelCoreObject obj = UModel.parseUModelJson(json);
        assertNotNull(obj);
        assertTrue(obj instanceof PrometheusV100);
        assertEquals("prometheus", obj.getKind());

        PrometheusV100 typed = (PrometheusV100) obj;
        SchemaV1 schema = typed.getSchema();
        assertNotNull(schema);
        assertEquals("umodel.aliyun.com", schema.getUrl());
        assertEquals("v1.0.0", schema.getVersion());

        MetadataV1 metadata = typed.getMetadata();
        assertNotNull(metadata);
        assertEquals("test_prometheus", metadata.getName());
        assertEquals("test.domain", metadata.getDomain());
        assertEquals("ga", metadata.getLaunchStage());

        SemanticStringV1 displayName = metadata.getDisplayName();
        assertNotNull(displayName);
        assertEquals("Test prometheus", displayName.getEnUs());

        Map<String, String> tags = metadata.getTags();
        assertNotNull(tags);
        assertEquals("test", tags.get("category"));
    }

    @Test
    public void testRunbookLinkJsonParsing() {
        String json = "{\"kind\":\"runbook_link\",\"schema\":{\"url\":\"umodel.aliyun.com\",\"version\":\"v1.0.0\"},\"metadata\":{\"name\":\"test_runbook_link\",\"display_name\":{\"zh_cn\":\"测试runbook_link\",\"en_us\":\"Test runbook_link\"},\"domain\":\"test.domain\",\"launch_stage\":\"ga\",\"tags\":{\"category\":\"test\"}},\"spec\":{\"_data\":\"placeholder\"}}";

        UModelCoreObject obj = UModel.parseUModelJson(json);
        assertNotNull(obj);
        assertTrue(obj instanceof RunbookLinkV100);
        assertEquals("runbook_link", obj.getKind());

        RunbookLinkV100 typed = (RunbookLinkV100) obj;
        SchemaV1 schema = typed.getSchema();
        assertNotNull(schema);
        assertEquals("umodel.aliyun.com", schema.getUrl());
        assertEquals("v1.0.0", schema.getVersion());

        MetadataV1 metadata = typed.getMetadata();
        assertNotNull(metadata);
        assertEquals("test_runbook_link", metadata.getName());
        assertEquals("test.domain", metadata.getDomain());
        assertEquals("ga", metadata.getLaunchStage());

        SemanticStringV1 displayName = metadata.getDisplayName();
        assertNotNull(displayName);
        assertEquals("Test runbook_link", displayName.getEnUs());

        Map<String, String> tags = metadata.getTags();
        assertNotNull(tags);
        assertEquals("test", tags.get("category"));
    }

    @Test
    public void testRunbookSetJsonParsing() {
        String json = "{\"kind\":\"runbook_set\",\"schema\":{\"url\":\"umodel.aliyun.com\",\"version\":\"v1.0.0\"},\"metadata\":{\"name\":\"test_runbook_set\",\"display_name\":{\"zh_cn\":\"测试runbook_set\",\"en_us\":\"Test runbook_set\"},\"domain\":\"test.domain\",\"launch_stage\":\"ga\",\"tags\":{\"category\":\"test\"}},\"spec\":{\"_data\":\"placeholder\"}}";

        UModelCoreObject obj = UModel.parseUModelJson(json);
        assertNotNull(obj);
        assertTrue(obj instanceof RunbookSetV100);
        assertEquals("runbook_set", obj.getKind());

        RunbookSetV100 typed = (RunbookSetV100) obj;
        SchemaV1 schema = typed.getSchema();
        assertNotNull(schema);
        assertEquals("umodel.aliyun.com", schema.getUrl());
        assertEquals("v1.0.0", schema.getVersion());

        MetadataV1 metadata = typed.getMetadata();
        assertNotNull(metadata);
        assertEquals("test_runbook_set", metadata.getName());
        assertEquals("test.domain", metadata.getDomain());
        assertEquals("ga", metadata.getLaunchStage());

        SemanticStringV1 displayName = metadata.getDisplayName();
        assertNotNull(displayName);
        assertEquals("Test runbook_set", displayName.getEnUs());

        Map<String, String> tags = metadata.getTags();
        assertNotNull(tags);
        assertEquals("test", tags.get("category"));
    }

    @Test
    public void testSlsEntitystoreJsonParsing() {
        String json = "{\"kind\":\"sls_entitystore\",\"schema\":{\"url\":\"umodel.aliyun.com\",\"version\":\"v1.0.0\"},\"metadata\":{\"name\":\"test_sls_entitystore\",\"display_name\":{\"zh_cn\":\"测试sls_entitystore\",\"en_us\":\"Test sls_entitystore\"},\"domain\":\"test.domain\",\"launch_stage\":\"ga\",\"tags\":{\"category\":\"test\"}},\"spec\":{\"_data\":\"placeholder\"}}";

        UModelCoreObject obj = UModel.parseUModelJson(json);
        assertNotNull(obj);
        assertTrue(obj instanceof SlsEntitystoreV100);
        assertEquals("sls_entitystore", obj.getKind());

        SlsEntitystoreV100 typed = (SlsEntitystoreV100) obj;
        SchemaV1 schema = typed.getSchema();
        assertNotNull(schema);
        assertEquals("umodel.aliyun.com", schema.getUrl());
        assertEquals("v1.0.0", schema.getVersion());

        MetadataV1 metadata = typed.getMetadata();
        assertNotNull(metadata);
        assertEquals("test_sls_entitystore", metadata.getName());
        assertEquals("test.domain", metadata.getDomain());
        assertEquals("ga", metadata.getLaunchStage());

        SemanticStringV1 displayName = metadata.getDisplayName();
        assertNotNull(displayName);
        assertEquals("Test sls_entitystore", displayName.getEnUs());

        Map<String, String> tags = metadata.getTags();
        assertNotNull(tags);
        assertEquals("test", tags.get("category"));
    }

    @Test
    public void testSlsLogstoreJsonParsing() {
        String json = "{\"kind\":\"sls_logstore\",\"schema\":{\"url\":\"umodel.aliyun.com\",\"version\":\"v1.0.0\"},\"metadata\":{\"name\":\"test_sls_logstore\",\"display_name\":{\"zh_cn\":\"测试sls_logstore\",\"en_us\":\"Test sls_logstore\"},\"domain\":\"test.domain\",\"launch_stage\":\"ga\",\"tags\":{\"category\":\"test\"}},\"spec\":{\"_data\":\"placeholder\"}}";

        UModelCoreObject obj = UModel.parseUModelJson(json);
        assertNotNull(obj);
        assertTrue(obj instanceof SlsLogstoreV100);
        assertEquals("sls_logstore", obj.getKind());

        SlsLogstoreV100 typed = (SlsLogstoreV100) obj;
        SchemaV1 schema = typed.getSchema();
        assertNotNull(schema);
        assertEquals("umodel.aliyun.com", schema.getUrl());
        assertEquals("v1.0.0", schema.getVersion());

        MetadataV1 metadata = typed.getMetadata();
        assertNotNull(metadata);
        assertEquals("test_sls_logstore", metadata.getName());
        assertEquals("test.domain", metadata.getDomain());
        assertEquals("ga", metadata.getLaunchStage());

        SemanticStringV1 displayName = metadata.getDisplayName();
        assertNotNull(displayName);
        assertEquals("Test sls_logstore", displayName.getEnUs());

        Map<String, String> tags = metadata.getTags();
        assertNotNull(tags);
        assertEquals("test", tags.get("category"));
    }

    @Test
    public void testSlsMetricstoreJsonParsing() {
        String json = "{\"kind\":\"sls_metricstore\",\"schema\":{\"url\":\"umodel.aliyun.com\",\"version\":\"v1.0.0\"},\"metadata\":{\"name\":\"test_sls_metricstore\",\"display_name\":{\"zh_cn\":\"测试sls_metricstore\",\"en_us\":\"Test sls_metricstore\"},\"domain\":\"test.domain\",\"launch_stage\":\"ga\",\"tags\":{\"category\":\"test\"}},\"spec\":{\"_data\":\"placeholder\"}}";

        UModelCoreObject obj = UModel.parseUModelJson(json);
        assertNotNull(obj);
        assertTrue(obj instanceof SlsMetricstoreV100);
        assertEquals("sls_metricstore", obj.getKind());

        SlsMetricstoreV100 typed = (SlsMetricstoreV100) obj;
        SchemaV1 schema = typed.getSchema();
        assertNotNull(schema);
        assertEquals("umodel.aliyun.com", schema.getUrl());
        assertEquals("v1.0.0", schema.getVersion());

        MetadataV1 metadata = typed.getMetadata();
        assertNotNull(metadata);
        assertEquals("test_sls_metricstore", metadata.getName());
        assertEquals("test.domain", metadata.getDomain());
        assertEquals("ga", metadata.getLaunchStage());

        SemanticStringV1 displayName = metadata.getDisplayName();
        assertNotNull(displayName);
        assertEquals("Test sls_metricstore", displayName.getEnUs());

        Map<String, String> tags = metadata.getTags();
        assertNotNull(tags);
        assertEquals("test", tags.get("category"));
    }

    @Test
    public void testStorageLinkJsonParsing() {
        String json = "{\"kind\":\"storage_link\",\"schema\":{\"url\":\"umodel.aliyun.com\",\"version\":\"v1.0.0\"},\"metadata\":{\"name\":\"test_storage_link\",\"display_name\":{\"zh_cn\":\"测试storage_link\",\"en_us\":\"Test storage_link\"},\"domain\":\"test.domain\",\"launch_stage\":\"ga\",\"tags\":{\"category\":\"test\"}},\"spec\":{\"_data\":\"placeholder\"}}";

        UModelCoreObject obj = UModel.parseUModelJson(json);
        assertNotNull(obj);
        assertTrue(obj instanceof StorageLinkV100);
        assertEquals("storage_link", obj.getKind());

        StorageLinkV100 typed = (StorageLinkV100) obj;
        SchemaV1 schema = typed.getSchema();
        assertNotNull(schema);
        assertEquals("umodel.aliyun.com", schema.getUrl());
        assertEquals("v1.0.0", schema.getVersion());

        MetadataV1 metadata = typed.getMetadata();
        assertNotNull(metadata);
        assertEquals("test_storage_link", metadata.getName());
        assertEquals("test.domain", metadata.getDomain());
        assertEquals("ga", metadata.getLaunchStage());

        SemanticStringV1 displayName = metadata.getDisplayName();
        assertNotNull(displayName);
        assertEquals("Test storage_link", displayName.getEnUs());

        Map<String, String> tags = metadata.getTags();
        assertNotNull(tags);
        assertEquals("test", tags.get("category"));
    }

    @Test
    public void testTraceSetJsonParsing() {
        String json = "{\"kind\":\"trace_set\",\"schema\":{\"url\":\"umodel.aliyun.com\",\"version\":\"v1.0.0\"},\"metadata\":{\"name\":\"test_trace_set\",\"display_name\":{\"zh_cn\":\"测试trace_set\",\"en_us\":\"Test trace_set\"},\"domain\":\"test.domain\",\"launch_stage\":\"ga\",\"tags\":{\"category\":\"test\"}},\"spec\":{\"_data\":\"placeholder\"}}";

        UModelCoreObject obj = UModel.parseUModelJson(json);
        assertNotNull(obj);
        assertTrue(obj instanceof TraceSetV100);
        assertEquals("trace_set", obj.getKind());

        TraceSetV100 typed = (TraceSetV100) obj;
        SchemaV1 schema = typed.getSchema();
        assertNotNull(schema);
        assertEquals("umodel.aliyun.com", schema.getUrl());
        assertEquals("v1.0.0", schema.getVersion());

        MetadataV1 metadata = typed.getMetadata();
        assertNotNull(metadata);
        assertEquals("test_trace_set", metadata.getName());
        assertEquals("test.domain", metadata.getDomain());
        assertEquals("ga", metadata.getLaunchStage());

        SemanticStringV1 displayName = metadata.getDisplayName();
        assertNotNull(displayName);
        assertEquals("Test trace_set", displayName.getEnUs());

        Map<String, String> tags = metadata.getTags();
        assertNotNull(tags);
        assertEquals("test", tags.get("category"));
    }

    @Test
    public void testUModelInterfaceFunctions() {
        String metricJson = "{\"kind\":\"aliyun_prometheus\",\"schema\":{\"version\":\"v1.0.0\"},\"metadata\":{\"name\":\"test\"}}";
        UModelCoreObject metricObj = UModel.parseUModelJson(metricJson);
        assertTrue(UModel.isCoreObject(metricObj));
        assertFalse(UModel.isLinkObject(metricObj));
        assertNotNull(UModel.getObjectMetadata(metricObj));
        assertNotNull(UModel.getObjectSchema(metricObj));

        String linkJson = "{\"kind\":\"data_link\",\"schema\":{\"version\":\"v1.0.0\"},\"metadata\":{\"name\":\"test\"}}";
        UModelCoreObject linkObj = UModel.parseUModelJson(linkJson);
        assertTrue(UModel.isCoreObject(linkObj));
        assertTrue(UModel.isLinkObject(linkObj));

        LinkEndpoint[] endpoints = UModel.getLinkEndpoints(linkObj);
        assertNotNull(endpoints);
        assertEquals(2, endpoints.length);
    }
}